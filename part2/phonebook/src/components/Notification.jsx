const Notification = ({ message }) => {
    if (message === null) {
        return null
    }

    const addClass = () => {
        if (message.includes()) {

        }
    }

    return (
        <div className="message">
            { message }
        </div>
    )
}

export default Notification